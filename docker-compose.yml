# Base compose for the bot runtime.
#
# This project owns the runtime services and the Redis queue/cache service.
# PostgreSQL/MySQL and backups remain external infrastructure and must be
# provided through environment variables such as DATABASE_URL,
# PLATFORM_DATABASE_URL and tenant DB allocation URLs.

services: {}

networks:
  internal:
    driver: bridge
