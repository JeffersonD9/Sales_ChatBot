# Base compose for the bot runtime.
#
# This project does not own database or Redis containers.
# PostgreSQL/MySQL, Redis and backups are external infrastructure and must be
# provided through environment variables such as DATABASE_URL, REDIS_URL,
# PLATFORM_DATABASE_URL and tenant DB allocation URLs.

services: {}

networks:
  internal:
    driver: bridge
