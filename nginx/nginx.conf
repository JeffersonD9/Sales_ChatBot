events {
  worker_connections 1024;
}

http {
  # Seguridad básica
  server_tokens off;
  add_header X-Frame-Options "DENY";
  add_header X-Content-Type-Options "nosniff";
  add_header Referrer-Policy "no-referrer";

  # Rate limiting global
  limit_req_zone $binary_remote_addr zone=webhook:10m rate=20r/s;

  # ── HTTP → HTTPS redirect ─────────────────────────────────────────────
  server {
    listen 80;
    server_name _;

    # ACME challenge (certbot)
    location /.well-known/acme-challenge/ {
      root /var/www/certbot;
    }

    location / {
      return 301 https://$host$request_uri;
    }
  }

  # ── HTTPS principal ───────────────────────────────────────────────────
  # Descomentar cuando tengas el certificado de Let's Encrypt:
  #
  # server {
  #   listen 443 ssl;
  #   server_name bots.jesttech.com;
  #
  #   ssl_certificate     /etc/letsencrypt/live/bots.jesttech.com/fullchain.pem;
  #   ssl_certificate_key /etc/letsencrypt/live/bots.jesttech.com/privkey.pem;
  #   ssl_protocols TLSv1.2 TLSv1.3;
  #   ssl_ciphers HIGH:!aNULL:!MD5;
  #
  #   # Webhooks de Meta (todos los tenants por slug)
  #   location /webhook/ {
  #     limit_req zone=webhook burst=50 nodelay;
  #     proxy_pass         http://app:3000;
  #     proxy_set_header   Host $host;
  #     proxy_set_header   X-Real-IP $remote_addr;
  #     proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
  #     proxy_read_timeout 10s;
  #   }
  #
  #   # Admin API — restringir a tu IP
  #   location /admin/ {
  #     allow YOUR.VPS.IP.HERE;  # tu IP fija, o comentar para solo API key
  #     deny all;
  #     proxy_pass http://app:3000;
  #     proxy_set_header Host $host;
  #   }
  #
  #   # Health check y demo
  #   location / {
  #     proxy_pass http://app:3000;
  #     proxy_set_header Host $host;
  #   }
  # }
}
